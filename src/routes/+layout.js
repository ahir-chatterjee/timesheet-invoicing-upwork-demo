// This enables SPA mode for the static adapter
export const prerender = false; // Disable prerendering since we have dynamic routes
export const ssr = false;

// Enable trailingSlash for consistency
export const trailingSlash = 'never'; 